-r base.txt
matplotlib>=3.7.0
Pillow>=10.0.0

# Data pipeline dependencies
aiohttp>=3.9.0
trafilatura>=1.6.0          # web text extraction for Common Crawl
beautifulsoup4>=4.12.0      # HTML parsing fallback
lxml>=5.0.0                 # fast HTML parser for BS4

# Optional — better language detection
# fasttext>=0.9.2           # uncomment and download lid.176.bin

# Optional — model-based toxicity filtering
# detoxify>=0.5.0           # uncomment to enable use_model:true
